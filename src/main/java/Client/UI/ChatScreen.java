package Client.UI;

import javax.swing.*;
import java.awt.*;

public class ChatScreen extends JPanel {

    public ChatScreen(LU_Connect_App luConnectApp){
        setLayout(new BorderLayout());

        JLabel title = new JLabel(luConnectApp.getClient())
    }
}
